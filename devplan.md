# CodeAtlas 开发计划（devplan）

## 1. 项目目标

在中文开发者市场提供一张可执行的「AI 编程工具选型 → 能力使用 → 工作流跑通 → 合理购买」地图，核心交付为：工具事实页、对比决策页、SOP 教程页、可复制模板页，以及 Use/Buy 聚合引导页。

## 2. 范围与约束

- 采用 `Next.js App Router + MDX` 作为内容引擎，所有内容可版本化、可追溯。
- 结论优先：对比页必须给出明确推荐与理由。
- 合规底线：购买信息可追溯、推广链接必须明确标注。
- 性能目标：首屏 < 2s、站内搜索输入后“可感知”即时反馈（本地过滤/缓存）。

## 3. 功能模块清单（PRD 对齐）

- 首页（Home）
- 工具（Tools）：列表 + 工具详情
- 对比（Compare）：列表 + 对比详情
- 教程（Tutorials）：列表 + 教程详情（SOP 模板）
- 模板（Templates）：列表 + 模板详情（一键复制）
- 使用（Use）：聚合引导页
- 购买（Buy）：聚合引导页
- 全局搜索：跨工具/对比/教程/模板的聚合搜索入口

## 4. 技术方案（高层）

### 4.1 内容存储与渲染

- 内容存储：`src/content/{tools|compare|tutorials|templates}/*.mdx`
- 解析：`gray-matter` 读取 frontmatter + 正文
- 渲染：`next-mdx-remote`（RSC）渲染正文
- 约束：对关键 frontmatter 做运行时校验，错误在构建阶段暴露

### 4.2 数据模型（内容域）

- 工具：能力、适用/不适用人群、价格/隐私、关联内容、更新时间、版本适用
- 对比：结论（新手/专业/团队）、维度评分、场景对比、成本与风险、最终建议
- 教程：SOP（目标/适用工具/步骤/关键技巧/失败修复/验收标准）
- 模板：用途、适用工具、一键复制模板体、变量说明、注意事项

### 4.3 搜索

- 后端：`/api/search-index` 返回轻量索引（title/description/keywords/updatedAt）
- 前端：首次打开时拉取索引并缓存，后续在本地做增量过滤，保证输入即时响应

## 5. 任务分解与里程碑

### M0：可浏览骨架（1-2 天）

- [ ] 路由：`/tools /compare /tutorials /templates /use /buy`
- [ ] 列表页：能展示最小字段（标题/描述/更新时间）
- [ ] 详情页：能渲染 MDX 正文 + 展示关键 frontmatter
- [ ] 首页：改为从内容动态取“热门/最新”

### M1：内容闭环（3-5 天）

- [ ] 工具详情页加入“用/买”区块（入门教程跳转 + 购买卡片）
- [ ] 对比页完成“结论先行 + 成本与风险”模块
- [ ] 教程页按 SOP 模板渲染（结构化区块）
- [ ] 模板页支持一键复制 + 变量提示

### M2：质量与增长（持续）

- [ ] 搜索：全站可用，支持键盘 `⌘/Ctrl+K`
- [ ] SEO：metadata/OG、内链策略、sitemap（按需要）
- [ ] 规范：内容更新 SOP、评测方法学、推广声明模板
- [ ] 自动化：lint/build 检查；后续引入测试框架（视仓库现状）

### M3：发布门禁与线上巡检（本轮）

- [x] GitHub CI：补齐 `lint + build` 基础门禁
- [x] 评审门禁：新增 `CODEOWNERS` 与 PR 模板
- [x] 分支保护建议：新增 `docs/github-branch-protection.md`
- [x] 发布后健康检查：新增脚本 `scripts/post-deploy-healthcheck.mjs`
- [x] 自动触发：新增工作流 `.github/workflows/post-deploy-healthcheck.yml`
- [ ] 补仓库变量 `PRODUCTION_BASE_URL`（待在 GitHub 仓库 Variables 配置）

## 6. 风险评估

- 内容风险：价格/政策变动频繁 → 必须在页面显著展示 `updatedAt` 与来源入口
- 合规风险：affiliate 争议 → 必须强制展示推广声明 + 方法学透明
- 维护成本：字段过多导致写作阻力 → 分层：必填/可选字段，逐步完善

## 7. 验收口径（本阶段）

- 能在本地 `npm run dev` 浏览全部栏目
- `npm run build` 通过（内容校验失败会阻断构建）
- 核心页面均显示更新时间与适用版本信息
